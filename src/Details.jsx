export function Details({ details, setTitle }) {
  return (
    <div>
      <h1>{details.title}</h1>
      <div>{details.description}</div>
      <button onClick={() => se}>click</button>
    </div>
  );
}
